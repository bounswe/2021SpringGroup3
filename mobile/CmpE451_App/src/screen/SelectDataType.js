import React from 'react';
import {View} from 'react-native';

export default function SelectDataType(props, {navigation}) {
  //console.log(this.props.navigation.state.params.communityId);
  return <View />;
}
